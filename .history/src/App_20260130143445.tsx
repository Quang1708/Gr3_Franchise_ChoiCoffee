import { Suspense } from "react"
import { BrowserRouter } from "react-router-dom"

function App() {

  return (
    <BrowserRouter>
      <Suspense>
        <Routes>
           {/* Admin */}
        </Routes>
      </Suspense>
    </BrowserRouter>
  )
}

export default App
