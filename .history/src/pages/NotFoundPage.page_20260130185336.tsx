import React from 'react'
import PropTypes from 'prop-types'

const NotFoundPage.page = props => {
  return (
    <div>NotFoundPage.page</div>
  )
}

NotFoundPage.page.propTypes = {}

export default NotFoundPage.page