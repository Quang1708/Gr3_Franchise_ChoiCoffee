import React from 'react'
import PropTypes from 'prop-types'

const ClientAuth.route = props => {
  return (
    <div>ClientAuth.route</div>
  )
}

ClientAuth.route.propTypes = {}

export default ClientAuth.route