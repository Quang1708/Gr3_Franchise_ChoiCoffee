export type CategoryFranchise = {
  id: string;
  is_active: boolean;
  is_deleted: boolean;
  created_at: string;
  updated_at: string;
  category_id: string;
  franchise_id: string;
  display_order: number;
}